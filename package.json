{
  "name": "property-read-write-codelens",
  "displayName": "Property Read/Write CodeLens",
  "description": "Displays CodeLens read/write counts for class properties in JavaScript and TypeScript",
  "publisher": "Lepasoft",
  "version": "1.0.7",
  "repository": {
    "type": "git",
    "url": "https://github.com/jonlepage/codelenreadwrite"
  },
  "bugs": {
    "url": "https://github.com/jonlepage/codelenreadwrite/issues"
  },
  "homepage": "https://marketplace.visualstudio.com/items?itemName=Lepasoft.property-read-write-codelens",
  "keywords": [
    "codelens",
    "property",
    "set/get",
    "read",
    "write",
    "references",
    "typescript",
    "javascript",
    "codelens read write",
    "telemetry",
    "class"
  ],
  "categories": [
    "Visualization"
  ],
  "engines": {
    "vscode": "^1.60.0"
  },
  "activationEvents": [
    "onLanguage:typescript",
    "onLanguage:javascript"
  ],
  "main": "./propertyReadWriteCodelens.js",
  "contributes": {
    "configuration": {
      "title": "Property Read Write CodeLens",
      "properties": {
        "propertyReadWriteCodelens.readEmoji": {
          "type": "string",
          "default": "📖",
          "description": "Emoji to use for read references in CodeLens."
        },
        "propertyReadWriteCodelens.writeEmoji": {
          "type": "string",
          "default": "✏️",
          "description": "Emoji to use for write references in CodeLens."
        },
        "propertyReadWriteCodelens.readAffix": {
          "type": "string",
          "default": "reads",
          "description": "Text/affix to display after the read count (e.g. 'reads', 'lectures', '読み取り')."
        },
        "propertyReadWriteCodelens.writeAffix": {
          "type": "string",
          "default": "writes",
          "description": "Text/affix to display after the write count (e.g. 'writes', 'écritures', '書き込み')."
        },
        "propertyReadWriteCodelens.showReads": {
          "type": "boolean",
          "default": true,
          "description": "Enable display of read references in CodeLens."
        },
        "propertyReadWriteCodelens.showWrites": {
          "type": "boolean",
          "default": true,
          "description": "Enable display of write references in CodeLens."
        }
      }
    },
    "commands": [
      {
        "command": "propertyReadWriteCodelens.refresh",
        "title": "Refresh Property Read/Write CodeLens"
      }
    ]
  },
  "license": "MIT",
  "scripts": {
    "build": "vsce package --out release"
  },
  "icon": "./public/icon.webp"
}